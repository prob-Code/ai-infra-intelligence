global:
  scrape_interval: 15s

scrape_configs:

  - job_name: "prometheus"
    static_configs:
      - targets:
          - "ai-prometheus:9090"

  - job_name: "node"
    static_configs:
      - targets:
          - "ai-node-exporter:9100"
